#include <stdio.h>
// logical operators
//  && - and , || - or , ! - not

int main()
{
    int a = 0, b = 1;

    printf("The value of a and b is %d\n", a && b);
    printf("The value of a and b is %d\n", a || b);
    printf("The value of a and b is %d\n", !a);

    return 0;
}